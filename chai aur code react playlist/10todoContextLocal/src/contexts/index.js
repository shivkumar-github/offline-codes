export { ToDoContext, ToDoContextProvider, useToDo } from './ToDoContexts'
// no need of index.js in this project but for projects with multiple contexts we import them to a single file index.js and then import them.